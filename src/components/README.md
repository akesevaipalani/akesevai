# Shared Components

Reusable UI pieces currently live near their feature usage in `App.jsx` and can be extracted here as the portal grows. The existing shared visual patterns include service cards, notification cards, form rows, modals and the customer dashboard.
